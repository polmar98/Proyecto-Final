const initialState = {
    orderId: null,
    state: ""
}

const checkoutReducer = (state = initialState, action) => {
    switch (action.type) {
        // case GET_STATUS:
        //     return{...state};

        default:
            return {...state}
    }
}